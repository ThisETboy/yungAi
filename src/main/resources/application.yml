# ============================================================
# SmartHub 全局配置
# 通过 spring.profiles.active 指定环境配置文件
# ============================================================

server:
  port: 8080

spring:
  application:
    name: smarthub
  profiles:
    active: dev  # 当前激活的环境：dev / test / prod
